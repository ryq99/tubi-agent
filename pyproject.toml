[project]
name = "tubi-agent"
version = "0.1.0"
description = "Scrape Tubi homepage carousels and Sony Pictures catalog metadata."
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
]
